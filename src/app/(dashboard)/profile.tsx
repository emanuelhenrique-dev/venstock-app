import { Text, View } from 'react-native';

export default function User() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 32 }}>Bem vindo User</Text>
    </View>
  );
}
